Inset text field with an uppercase tracked label and brass focus ring. Supports `hint`, `error`, `iconLeft`.

```jsx
<Input label="Date of birth" placeholder="DD / MM / YYYY" />
<Input label="Email" type="email" error="Enter a valid email" />
```
